This project converts articles in csv format and converts them into json, which will be fed to next stage of pre-processing.

Input: "Data" directory with csv files
Output: "Results" directory with json files