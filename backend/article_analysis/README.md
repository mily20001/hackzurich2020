This project uses geoparsepy (https://github.com/stuartemiddleton/geoparsepy) to predict mentioned locations in articles (in json format from ParseArticles project).

Input: "data_json" directiory with json files
Output: "data_json_hydrated" directiory with json files. 
